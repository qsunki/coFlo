spring:
  application:
    name: coflo

  profiles:
    default: local

  output:
    ansi:
      enabled: always

gitlab:
  api:
    project-list: /api/v4/projects
    user-info: /api/v4/user

crypto-key: ${CRYPTO_KEY}