spring:
  application:
    name: coflo

  profiles:
    default: local

  output:
    ansi:
      enabled: always

gitlab:
  api:
    project-list: /api/v4/projects
    project-search: /api/v4/search?scope=projects&search=
