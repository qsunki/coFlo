import SignupForm from '@components/Signup/SignupForm.tsx';

const SignupContent = () => {
  return (
    <div className="flex flex-row justify-center items-center w-full h-full mx-auto px-20">
      <SignupForm />
    </div>
  );
};

export default SignupContent;
